<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.view.mapper.CustomerMapper">

    <select id="findAll" resultType="com.example.view.model.Customer">
        SELECT id, name, email, phone, address, created_at
        FROM customers
        ORDER BY name
    </select>

    <select id="findById" resultType="com.example.view.model.Customer">
        SELECT id, name, email, phone, address, created_at
        FROM customers
        WHERE id = #{id}
    </select>

    <select id="findByEmail" resultType="com.example.view.model.Customer">
        SELECT id, name, email, phone, address, created_at
        FROM customers
        WHERE email LIKE CONCAT('%', #{email}, '%')
        ORDER BY name
    </select>

    <insert id="insert" parameterType="com.example.view.model.Customer">
        INSERT INTO customers (name, email, phone, address, created_at)
        VALUES (#{name}, #{email}, #{phone}, #{address}, NOW())
    </insert>

    <update id="update" parameterType="com.example.view.model.Customer">
        UPDATE customers
        SET name = #{name}, email = #{email}, phone = #{phone}, address = #{address}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM customers WHERE id = #{id}
    </delete>

</mapper>
