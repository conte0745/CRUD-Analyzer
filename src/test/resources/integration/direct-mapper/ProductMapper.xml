<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.direct.mapper.ProductMapper">

    <select id="findAll" resultType="com.example.direct.model.Product">
        SELECT id, name, category, price
        FROM products
        ORDER BY id
    </select>

    <select id="findById" resultType="com.example.direct.model.Product">
        SELECT id, name, category, price
        FROM products
        WHERE id = #{id}
    </select>

    <select id="findByCategory" resultType="com.example.direct.model.Product">
        SELECT id, name, category, price
        FROM products
        WHERE category = #{category}
        ORDER BY name
    </select>

    <insert id="insert" parameterType="com.example.direct.model.Product">
        INSERT INTO products (name, category, price, created_at)
        VALUES (#{name}, #{category}, #{price}, NOW())
    </insert>

    <update id="update" parameterType="com.example.direct.model.Product">
        UPDATE products
        SET name = #{name}, category = #{category}, price = #{price}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM products WHERE id = #{id}
    </delete>

    <delete id="deleteByCategory">
        DELETE FROM products WHERE category = #{category}
    </delete>

</mapper>
